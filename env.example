# Servidor
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# Banco de dados PostgreSQL
# Exemplo Railway/Neon/Supabase:
# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# Autenticacao
JWT_SECRET="troque-por-uma-chave-grande-e-segura"
JWT_REFRESH_SECRET="troque-por-outra-chave-grande-e-segura"
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d
BCRYPT_ROUNDS=12

# Redis opcional
REDIS_URL=
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
# Quando REDIS_URL estiver definido, backend usa Redis para brute force e metricas distribuidas.

# E-mail (obrigatório para enviar verificação de conta, reset de senha, etc.)
# Sem SMTP_HOST + SMTP_USER + SMTP_PASS preenchidos, o backend registra aviso e não envia.
# Ex.: Gmail usa smtp.gmail.com + "senha de app"; em dev use Mailtrap ou Ethereal.
#
# SMTP_USER / EMAIL_FROM = conta que AUTENTICA no SMTP (remetente). Não é o destinatário.
# O destinatário do e-mail de boas-vindas é sempre o e-mail que o cliente cadastrou (campo "to").
# O script npm run email:templates-demo envia para o endereço que você passar na linha de comando.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_FROM_NAME=AgilBank
# URL do front onde existem /banco/confirmar-email.html e reset (ex.: Vite http://127.0.0.1:5173)
FRONTEND_URL=http://127.0.0.1:5173

# Upload opcional
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# Pagamentos/PIX em modo demo
PIX_PROVIDER=demo
GERENCIANET_CLIENT_ID=
GERENCIANET_CLIENT_SECRET=
GERENCIANET_SANDBOX=true

# Monitoramento opcional
SENTRY_DSN=

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX_REQUESTS=20
AUTH_MAX_FAILED_ATTEMPTS=5
AUTH_FAILED_WINDOW_MS=900000
AUTH_BLOCK_DURATION_MS=900000
OPS_METRICS_TTL_SECONDS=86400

# CORS
# Em producao, lista separada por virgula (obrigatorio para browser chamar a API).
# Tambem e possivel usar FRONTEND_URL: a origem dela e acrescentada automaticamente.
# Ex.: CORS_ORIGIN=https://app.agilbank.com,http://127.0.0.1:5173
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# Chaves internas (nao expor no frontend)
OPS_METRICS_INTERNAL_KEY=
CARD_APPROVAL_INTERNAL_KEY=
